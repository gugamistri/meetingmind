/**
 * Main Application Component
 * 
 * Now serves as the entry point for the Dashboard-focused application
 * with React Router integration and proper navigation.
 */

import { AppRouter } from './router/AppRouter';

function App() {
  return <AppRouter />;
}

export default App;
